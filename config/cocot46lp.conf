# 32 kHz clock: BMP has no external 32 kHz crystal.
CONFIG_CLOCK_CONTROL_NRF_K32SRC_RC=y

# P0.10 (NFC2, used as col6/PIN14) is an NFC antenna pin by default on
# nRF52840 and WILL NOT WORK as GPIO without this config.
# First boot after flashing will write UICR and auto-reset once.
CONFIG_NFCT_PINS_AS_GPIOS=y

# BOARD 2 PREP (BLE off): unlock APPROTECT + confirm the 2nd BMP boots ZMK and
# keys work, before we solder SWD to it. Layout is 0x26000 (SoftDevice kept).
# approtect_unlock.c writes UICR.APPROTECT=0x5A; NRF_APPROTECT_USE_UICR keeps the
# chip debug-unlocked across resets so the ST-Link can attach for debugging.
CONFIG_ZMK_BLE=n
CONFIG_NRF_APPROTECT_USE_UICR=y

CONFIG_ZMK_KSCAN_MATRIX_POLLING=y

# USB CDC ACM for debug logging over USB serial
CONFIG_LOG=y
CONFIG_USB_CDC_ACM=y
CONFIG_ZMK_USB_LOGGING=y
CONFIG_ZMK_LOG_LEVEL_DBG=y
CONFIG_LOG_MODE_IMMEDIATE=y
